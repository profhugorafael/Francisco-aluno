# O que já conhecemos em `C`

1. Incluir bibliotecas

```c
#include <nome_da_biblioteca>
#include <nome_da_biblioteca.h>
```

2. Criar uma função principal (`main()`)

```c
int main() {
  // nosso código
  return 0;
}
```